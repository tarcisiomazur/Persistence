namespace Persistence
{
    public enum KeyType
    {
        PrimaryKey,
        ForeignKey,
        Null
    }
    public enum ProcedureTypeCommand
    {
        Insert,
        Delete,
        Update
    }
}